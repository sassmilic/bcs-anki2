# TODO

- [ ] Retry image generation when the prompt is rejected by the AI safety filter — regenerate the image prompt and try again instead of failing the word
- [ ] Review prompts — audit and tune all LLM prompts (definition, image, image source) for output quality
